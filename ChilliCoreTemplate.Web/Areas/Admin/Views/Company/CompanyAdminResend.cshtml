@model CompanyUserViewModel
@using (Html.BeginFormCustom(Mvc.Admin.Company_Admin_Resend, htmlAttributes: new { @class = "form-horizontal" }))
{
    @Html.HiddenFor(m => m.Id)
    @Html.HiddenFor(m => m.UserId)
    <div class="modal-body">
        @Html.ValidationSummary()
        <p>Are you sure you want resend the invite for <b>@Model.Name</b>?</p>
    </div>
    <div class="modal-footer">
        @await Html.ButtonSubmitAsync(new Template_Button { Style = ButtonStyle.Primary, Text = "Yes, resend the invite" })
        @await Html.ButtonAsync(FieldTemplateHelper.ButtonModalDismissOptions)
    </div>
}
<script>
    $('#@Mvc.Admin.Company_Admin_Resend.GetFormId()')
        .ajaxForm({ target: '#@Mvc.Admin.Company_Admin_Resend.GetModalId()_content' })
        .done(function () {
            SearchTable(userTable);
            $('#@Mvc.Admin.Company_Admin_Resend.GetModalId()').modal('hide');
        });
</script>